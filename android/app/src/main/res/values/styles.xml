<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- Splash do sistema (Android 12+). Mostra o ícone estático (logo-icon-512)
         sobre o vermelho da marca e entrega à SplashActivity (postSplashScreenTheme),
         que corre a animação Lottie. Em Android < 12, a androidx core-splashscreen
         emula este comportamento. -->
    <style name="Theme.App.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_red</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.App.SplashHost</item>
    </style>

    <!-- Tema da SplashActivity depois do splash do SO: fundo vermelho a toda a
         largura, para não haver flash branco antes do Lottie aparecer. -->
    <style name="Theme.App.SplashHost" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/splash_red</item>
        <item name="android:statusBarColor">@color/splash_red</item>
        <item name="android:navigationBarColor">@color/splash_red</item>
    </style>
</resources>
