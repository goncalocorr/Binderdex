{
  "name": "functions",
  "description": "Cloud Functions do Binderdex (notificações push)",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "node --test"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0",
    "googleapis": "^144.0.0"
  }
}
